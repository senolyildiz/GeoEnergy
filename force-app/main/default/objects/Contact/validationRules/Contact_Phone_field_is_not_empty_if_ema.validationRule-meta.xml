<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Contact_Phone_field_is_not_empty_if_ema</fullName>
    <active>true</active>
    <description>Contact Phone field is not empty if email communication is opted out by customer.
So that I can be sure that the remaining commutation channel is functional.</description>
    <errorConditionFormula>isblank(Phone ) &amp;&amp;  HasOptedOutOfEmail</errorConditionFormula>
    <errorMessage>Be sure that Contact Phone field is not empty if email communication is opted out by customer</errorMessage>
</ValidationRule>
